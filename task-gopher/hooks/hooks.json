{
  "description": "task-gopher — SessionStart injects the full Haiku-delegation directive on startup, resume, clear, and post-compaction (so it survives compaction); UserPromptSubmit injects a compact reminder each turn. Both are no-ops unless the plugin is toggled ON.",
  "hooks": {
    "SessionStart": [
      {
        "matcher": "startup|resume|clear|compact",
        "hooks": [
          {
            "type": "command",
            "command": "node \"${CLAUDE_PLUGIN_ROOT}/hooks/sessionstart.mjs\""
          }
        ]
      }
    ],
    "UserPromptSubmit": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "node \"${CLAUDE_PLUGIN_ROOT}/hooks/userpromptsubmit.mjs\""
          }
        ]
      }
    ]
  }
}
