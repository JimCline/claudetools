---
description: Note a PR's diff as you go, then compile a reviewer's guide from those notes.
argument-hint: "note <text> [--watch <flag>] | note --skip <why> | status | guide [--pr]"
---

The user ran `/review-guide` with argument: `$ARGUMENTS`.

Invoke the `review-guide` skill (this plugin, `review-guide:review-guide`) to handle it,
passing `$ARGUMENTS` through, and follow that skill's instructions exactly.
