---
description: Define, edit, or inspect the agent-hierarchy roster (which roles exist, their model/effort/route), spawn the roster as a live Team, or disband one.
argument-hint: "[show|init|add|edit|remove|layout|create [auto|manual]|disband]"
---

The user ran `/agent-roster` with argument: `$ARGUMENTS`.

Invoke the `agent-roster` skill (this plugin, `ah:agent-roster`) to handle it,
passing `$ARGUMENTS` through, and follow that skill's instructions exactly.
