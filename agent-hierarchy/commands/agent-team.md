---
description: Stand up, inspect, reshape, or tear down a live Team of agent sessions from the existing agent-hierarchy roster.
argument-hint: "[create [auto|manual]|spawn-one <role>|spawn-ad-hoc <role>|dismiss <name>|disband|teams|resync|move|adopt|reap|history]"
---

The user ran `/agent-team` with argument: `$ARGUMENTS`.

Invoke the `agent-team` skill (this plugin, `ah:agent-team`) to handle it,
passing `$ARGUMENTS` through, and follow that skill's instructions exactly.
